# Problem running cosmos with this repository

The page looks empty


## Steps

1. npm install
2. Execute:
./node_modules/.bin/component-playground --components-path src/components/ --fixtures-path src/fixtures/
